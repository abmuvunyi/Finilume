<p><%= t('devise.mailer.unlock_instructions.hello', default: 'Hello') %> <%= @resource.email %>!</p>

<p><%= t('devise.mailer.unlock_instructions.locked_message', default: 'Your account has been locked due to an excessive number of unsuccessful sign in attempts.') %></p>

<p><%= t('devise.mailer.unlock_instructions.click_link', default: 'Click the link below to unlock your account:') %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.unlock_account', default: 'Unlock my account'), unlock_url(@resource, unlock_token: @token) %></p>
