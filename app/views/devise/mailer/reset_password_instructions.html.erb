<p><%= t('devise.mailer.reset_password_instructions.hello', default: 'Hello') %> <%= @resource.email %>!</p>

<p><%= t('devise.mailer.reset_password_instructions.request_message', default: 'Someone has requested a link to change your password. You can do this through the link below.') %></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.change_password_link', default: 'Change my password'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.ignore_if_not_requested', default: "If you didn't request this, please ignore this email.") %></p>
<p><%= t('devise.mailer.reset_password_instructions.password_wont_change', default: "Your password won't change until you access the link above and create a new one.") %></p>
