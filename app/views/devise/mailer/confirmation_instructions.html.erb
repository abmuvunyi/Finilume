<p><%= t('devise.mailer.confirmation_instructions.welcome', default: 'Welcome') %> <%= @email %>!</p>

<p><%= t('devise.mailer.confirmation_instructions.message', default: 'You can confirm your account email through the link below:') %></p>

<p><%= link_to t('devise.mailer.confirmation_instructions.confirm_my_account', default: 'Confirm my account'), confirmation_url(@resource, confirmation_token: @token) %></p>
